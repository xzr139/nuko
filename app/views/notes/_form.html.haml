= form_for @note do |f|
  - if @note.errors.any?
    #error_explanation
      %h2= "#{pluralize(@note.errors.count, "error")} prohibited this note from being saved:"
      %ul
        - @note.errors.full_messages.each do |msg|
          %li= msg
  .post-block
    .title
      = f.text_field :title, placeholder: 'title'
    .post-form
      = f.text_area :content
    .submit
      = f.submit 'Save', id: 'submit', class: 'btn btn-default'
