box: wercker/rvm

services:
    - wercker/mysql

build:
    steps:
        - rvm-use:
              version: 2.1.3

        - bundle-install

        - rails-database-yml:
            service: mysql

        - script:
            name: echo ruby information
            code: |
                echo "ruby version $(ruby --version) running"
                echo "from location $(which ruby)"
                echo -p "gem list: $(gem list)"

        - script:
            name: Set up db
            code: RAILS_ENV=test bundle exec rake db:migrate && RAILS_ENV=test bundle exec rake parallel:create && RAILS_ENV=test bundle exec rake parallel:prepare

        - script:
            name: Run RSpec
            code: bin/rake parallel:spec

    after-steps:
        - wantedly/pretty-slack-notify:
            webhook_url: $SLACK_WEBHOOK_URL
            channel: nuko
            username: nuko
